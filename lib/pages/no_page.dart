import 'package:flutter/material.dart';

class NoPage extends StatelessWidget {
  const NoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset("assets/img/tetot.png"),
    );
  }
}
